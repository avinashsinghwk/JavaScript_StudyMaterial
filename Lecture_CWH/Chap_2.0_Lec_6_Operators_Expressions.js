
        console.log("In this lecture we will study about Operators and Expressions");

        // --------- Arithmetic Operators ----------

        let a = 34;
        let b = 4;
        // console.log("a + b =", a + b)
        // console.log("a - b =", a - b)
        // console.log("a * b =", a * b)
        // console.log("a / b =", a / b)
        // console.log("a % b =", a % b)
        // console.log("a ** b =", a ** b)  // It will make a to the power b


        // ------ Increment & Decrement Operator -------

        // console.log("a-- =", a--);        // It will first write it then decrease it by 1
        // console.log(a);
        // console.log("a++ =", a++);        // It will first write it then imporve it by 1
        // console.log(a);

        // console.log("++a =", ++a);        // It will first imporve it by 1 then write it
        // console.log(a);
        // console.log("--a =", --a);        // It will first decrease it by 1 then write it
        // console.log(a);


        // -------- Assignment Operators ----------

        let c = 4;
        c += 5;    // Same as c = c + 5;
        c **= 2;
        // console.log(c);


        // ----------- Comparison Operators ----------

        let com1 = 6;
        let com2 = 7;
        // console.log("com1 > com2 is",com1 > com2)
        // console.log("com1 == com2 is",com1 == com2)
        // console.log("com1 != com2 is",com1 != com2)
        // console.log("com1 === com2 is",com1 === com2)   // value and type both should equal
        // console.log("com1 !== com2 is",com1 !== com2)   // not value equal or not type equal


        // --------- Logic Operators ----------

        let log1 = 5;
        let log2 = 6;
        // console.log(log1 < log2 && log1 == 5)
        // console.log(log1 > log2 || log1 == 5)
        // console.log(!true)

        // Bitwise Operator perform bit by bit operations on numbers.


        // -------- Comments in JavaScript ----------
        
        // Single Line Comment

        /* This is
        a multi
        Line Comment */